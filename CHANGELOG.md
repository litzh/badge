# 更新记录

## 0.9.0 — 2026-09-19

- 新增本地 `set_volume`、`set_brightness` 和 `schedule_shutdown` 工具，支持语音修改并保存设置。
- 音量 0–100%，亮度 1–100%；相对调节先读取当前保存值，默认增减 10 个百分点。
- 回答成功完成后倒计时 10 秒，通过 AXP2101 请求关机；回答失败、取消或新对话自动撤销，BOOT/触屏可取消。
- 主页显示倒计时与取消按钮；增加关机状态和取消接口、参数与状态机测试、静默实机设置验证脚本。

## 0.8.0 — 2026-09-19

- DeepSeek 对话增加本地只读工具 `read_device_state`，按需读取电池、姿态、芯片内部温度、音量亮度与 Wi-Fi 信号。
- 主循环提供带有效状态/采样年龄的传感器快照，语音线程使用可取消的队列；保留云端搜索、多工具结果及简短回答。
- 严格校验字段，限制工具次数/模型请求与时间预算；工具结果不含网络配置，失败或过期测量不作为零值。
- 新增工具遥测、静默文本联调模式、真实 JSON 协议与队列并发测试。

## 0.7.0 — 2026-09-18

- BOOT 专门控制对话：短按开始/提交/取消，长按不重复；PWR 短按控制亮屏/熄屏，后台问答继续。
- 新主页突出语音状态、录音计时和操作提示；设置页管理音量、亮度、Wi-Fi 配网和设备信息。
- 触屏按钮可见、按下高亮、松开执行、滑出取消；首次触摸仅唤醒，上下限按钮置灰。
- 手动熄屏不会被语音状态变化或任务完成打断；新增交互逻辑回归测试与离线布局预览。

## 0.6.1 — 2026-09-18

- 状态页增加音量减/加按钮，每次 10，范围 0–100；0 静音，播放过程中可调整。
- 音量保存在 NVS，重启保留；构建参数仅作为无保存值时的默认值。
- 增加 `POST /voice/volume`，状态提供当前音量、构建默认音量及独立的保存错误。

## 0.6.0 — 2026-09-18

- 设备端语音问答：16 kHz 录音、MiniMax ASR、DeepSeek Flash 服务端搜索与简短回答、MiniMax WAV 合成和 ES8311 播放。
- 构建时注入 MiniMax/DeepSeek 密钥、音色、模型、录音上限、音量及 UTF-8 TXT 系统提示词；默认内置音色 `male-qn-qingse`。
- 状态页底部点击开始/结束录音，处理和播放期间可取消；增加语音 HTTP/串口调试接口。
- 统一屏保采音与语音录放音的硬件所有权，对话期间保持唤醒，结束后恢复待机；保留电池保护和 NTP 校时。
- HTTPS 验证证书，音频和大型搜索响应使用 PSRAM，校验 WAV 完整性后播放；最多保留六轮对话，回答硬上限 160 字符。
- 当前为完整录音、完整回答和下载后播放；没有唤醒词、自动断句或全双工打断。

## 0.5.1 — 2026-09-18

- 每次 Wi-Fi 连接后使用阿里云/腾讯 NTP 校准 ESP 系统时间，正常每小时同步，60 秒超时切换服务器优先级重试。
- 状态页顶部显示北京时间，`/status.time_sync` 提供同步状态、带时区的当前时间、最近成功同步时间和错误原因。
- 未校时时明确显示等待状态；断网后继续走时，重启后重新校时，不假定有独立 RTC。
- 同步过程不阻塞等候网络，不唤醒屏幕或重置待机计时，保留 0.5.0 屏保与电池保护功能。
- 已编译、刷入并通过只读实机验证：NTP 同步成功、北京时间持续走动，轮询期间待机计时继续增长；电池/触摸/IMU、麦克风和粒子屏保初始化正常。

## 0.5.0 — 2026-09-18

- 分析首轮完整充放电数据：持续屏保到最后在线约 4 小时 40 分钟，首次充电读数到充满约 2 小时 41 分钟。
- 状态页显示 PMU 电量估计、充电/充满和低电提醒；电压持续偏低时进入节能和 PMU 保护关机。
- 屏保最长显示 5 分钟，随后面板休眠、停止采音和动画；保留联网查询与交互唤醒。
- 采集器记录熄屏/低电字段，兼容旧 CSV；附可复现分析脚本、曲线和管理策略文档。
- 保护采用连续新样本、迟滞、USB 与最终电压复核，不改变充电参数或电量计校准。
- 编译及主机测试通过，已刷入 0.5.0；实机验证 310 秒熄屏、采音/动画停止、遥测继续及 HTTP 唤醒恢复。
- 新策略下续航及真实低电保护断电效果需要下一轮实验确认。

## 0.4.2 — 2026-09-17

本次提交包含从初始固件到 0.4.2 的屏保和电池测试功能。

### 新增

- 10 秒无交互进入屏保，触摸或按键唤醒并恢复正常亮度。
- 80 粒子屏保融合麦克风宽频段能量、IMU 倾斜/旋转/摇晃和芯片温度。
- ES7210 在屏保期间以 16kHz 双通道采样，唤醒后停止，不保存或上传原始音频。
- 电池存在、VBUS 输入、充放电状态和 PMU 电量估计；温度、音频及动画运行状态。
- 本地 Python 电池记录器，输出 CSV、原始 JSONL 和离线实时 HTML 曲线，支持历史重绘。
- 主机测试、实机检查脚本，以及交互、屏保、电池记录、硬件和接口文档。

### 修复

- 状态页由每秒清空整屏改为缓存文字行并在内存中绘制，消除全屏文字闪烁。
- 屏保亮度上限从 24 提高至 128，解决实际面板上粒子几乎不可见的问题；
  正常亮度低于上限时保持原值。屏保以两行条带独立传屏。
- 构建脚本自动加载 `.env`，兼容旧的 `RLCD_WIFI_*` 默认网络变量。
- 提供 Apple Silicon 原生 Arduino ctags 构建脚本，构建缓存与固件产物目录分离。

### 验证与限制

- Core 3.3.2-cn 编译通过，已刷入并校验 USB 写入；实机动画约 10 帧/秒。
- 已验证 10 秒计时、唤醒、采音启停、温度和电池遥测；用户确认文字稳定、粒子可见。
- C++ 测试覆盖计时回绕、音频特征、粒子边界及条带传输；Python 测试覆盖电池记录与重绘。
- 温度是芯片内部温度；电量百分比为 PMU 估算。当前未启用语音聊天、扬声器播放或深度睡眠。
- 新版屏保的亮度、采音与渲染功耗不同于旧版，续航需在相同固件及使用条件下比较。
